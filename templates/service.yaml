apiVersion: v1
kind: Service
metadata:
  name: {{ include "welcome-page.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "welcome-page.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
